---
// 保留旧链接与公告预览入口；通知统一在当前首页抽屉展示。
const kind = Astro.url.searchParams.get('kind');
return Astro.redirect(`/?notifications=${kind === 'submission' || kind === 'announcement' ? kind : 'site'}`, 302);
---
