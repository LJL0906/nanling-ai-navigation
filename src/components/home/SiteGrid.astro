---
/** 紧凑站点网格，根据容器宽度自动排列。 */
import type { Site } from '../../lib/types';
import SiteCard from './SiteCard.astro';

interface Props {
  sites: Site[];
  label?: string;
}
const { sites, label } = Astro.props;
---
<ul class="nav-card-grid" aria-label={label}>
  {sites.map((site) => (
    <li><SiteCard site={site} /></li>
  ))}
</ul>

