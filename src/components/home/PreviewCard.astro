---
/** 资讯站点也使用紧凑卡片，保持所有导航卡片尺寸一致。 */
import type { Site } from '../../lib/types';
import SiteCard from './SiteCard.astro';
interface Props { site: Site; }
const { site } = Astro.props;
---
<SiteCard site={site} />
